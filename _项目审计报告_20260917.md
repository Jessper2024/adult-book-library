# 成人图书创建 · 安全与稳固审计报告

审计时间：2026-09-17 03:40
审计范围：`~/Life/成人图书创建/` 全项目 + `~/Life/01、源料_Pornstar csv/` 源料 +
`~/WorkBuddy/2026-09-15-13-19-03/` 12 个脚本（2591 行）+ `edge-batch-capture` skill

---

## 〇、结论摘要

| 维度 | 当前等级 | 目标等级 | 卡在哪 |
|---|---|---|---|
| **数据不可失** | ⚠️ **C**（有覆盖写风险） | A | epub 覆盖写无原子替换、源料覆盖式替换无留档 |
| **数据正确性** | ⚠️ **C+** | A | 占位图误杀、演员解析失败误判单人、看板进度错 |
| **可恢复性** | ⚠️ **C** | A | 明细 json 每批覆盖、无版本目录、备份落 /tmp |
| **并发安全** | ⚠️ **C** | B+ | 无文件锁，靠人工 `pgrep` |
| **容量安全** | 🔴 **D** | B | **磁盘可用仅 22.1 GB（9.7%）** |
| **文档/规则完备** | ✅ **A-** | A | skill 已很全，本次又补了第十二节 |
| **综合** | **C+** | **A-** | 主要短板在"写"的环节，链路读侧很稳 |

一句话：**读链路（源料 → CSV → 校对）已经很扎实，写链路（xhtml → epub）缺少失败保护，
容量是当前最硬的约束。**

---

## 一、资产盘点

| 项 | 数值 |
|---|---|
| 演员数 | 8 |
| 主 CSV 总条数 | 6,236（重构后权威值） |
| 单人 CSV 总条数 | 1,490 |
| xhtml 文稿 | 2,311 个 |
| epub | 85 本 |
| 项目体积 | **6.29 GB** |
| 磁盘可用 | **22.1 GB / 228 GB（9.7%）** ⚠️ |

体积分布：北原夏美 1.82 GB > 京香Julia 1.58 GB > 北条麻妃 1.12 GB > 中森玲子 0.79 GB >
木下 0.60 GB > 橘 0.16 GB > 白鳥 0.16 GB > 凪 0.06 GB

识别码唯一性：✅ **2,311 个 xhtml 全部唯一**，无 epub 图片覆盖风险。

---

## 二、缺陷清单

### 🔴 P0 — 数据安全（不可逆损失风险）

#### P0-1　epub 生成是覆盖写，无原子替换、无磁盘预检
- 位置：`sigil_epub_build.py:277` `zipfile.ZipFile(epub_path, "w")`
- 问题：**直接覆盖目标文件**。写到一半失败（磁盘满 / 内存不足 / 异常），
  **原 epub 已损毁**，且留下半截的坏文件。
- 现实威胁：磁盘只剩 22.1 GB，北原夏美大全集 1.08 GB、京香Julia 分卷多本，
  重建时一旦撑爆就是**书没了**。
- ✅ 修复：
  ```python
  # 1) 写前预检
  need = 估算体积; free = shutil.disk_usage(out).free
  if free < need * 1.5: raise SystemExit("空间不足")
  # 2) 写临时文件再原子替换
  tmp = epub_path + ".tmp"
  with zipfile.ZipFile(tmp, "w", ...) as z: ...
  os.replace(tmp, epub_path)      # 原子，失败时原文件完好
  ```

#### P0-2　源料 CSV 覆盖式替换，无版本留档
- 位置：`~/Life/01、源料_Pornstar csv/{演员}/` —— 新抓取的文件直接覆盖同名文件
- 问题：新一版抓取若缺页/失败，**老数据当场消失，无法回滚**。
  这正是 9/17 那 4 条"主 CSV 独有"的成因（9/16 版有、9/17 版没了）。
- ✅ 修复：新抓取先落 `…作品_YYYYmmdd-HHMM.csv`，条数/演員分布自检通过后再替换；
  旧版移入 `_历史版本/` 而非删除。

#### P0-3　`sigil_result.json` 每批覆盖，明细永久丢失
- 位置：`javbus_to_sigil.py:369` 路径写死在脚本目录
- 问题：跑第二批就覆盖第一批的逐条明细（哪条失败、哪条占位图）。
  SKILL.md 已记为"坑"，但只在文档里要求手动 `cp`，**没人记得住**。
- ✅ 修复：自动写 `sigil_result_YYYYmmdd-HHMMSS.json`，并软链 `sigil_result.json` 到最新。

---

### 🟠 P1 — 正确性（静默数据污染）

#### P1-1　占位图判定与文档不符，会误杀真实小图
- 位置：`javbus_to_sigil.py:296` `if len(data) <= PLACEHOLDER_SIZE`（2732）
- 文档（SKILL.md 六）写的是「同一作品**多张图大小完全相同**且 < 10KB」
- 实现是「**单张** ≤ 2732 字节就跳」
- 后果：**真实存在但体积小的样品图被静默丢弃**，且不会报错。
- ✅ 修复：改成先收集全部图片体积，仅当 `len(set(sizes)) == 1 and max(sizes) < 10240` 才整批跳过。

#### P1-2　样品图下载失败 → 整部作品失败，已下载图片全丢
- 位置：`javbus_to_sigil.py:294-301`，`fetch(u)` **无 try/except**（封面那处有）
- 后果：8 张图里第 7 张超时 → 前 6 张白下载，整部标记"失败"。
- ✅ 修复：单张包 try/except，失败只 `skipped += 1` 继续下一张。

#### P1-3　`esc()` 不转义引号，可破坏 XML 属性
- 位置：`javbus_to_sigil.py:207-208` 只转 `& < >`
- 第 235 行 `title="' + esc(cover_title) + '"` —— 标题含半角 `"` 时**属性提前闭合**，XML 直接坏。
- ✅ 修复：`esc()` 增加 `&quot;` / `&#39;`。

#### P1-4　解析不到演员 → 误判为「单人」，污染单人集合
- 位置：`javbus_to_sigil.py:272` `actors = info["actors"] or ["unknown-actor"]`
  → `len == 1` → 前缀判为 `单人`
- 后果：页面结构变化/反爬残留导致演员抓取失败时，该作品被**错误归入单人**，
  之后所有单人统计、单人 epub 都被污染。
- ✅ 修复：演员为空时不要 fallback 成 1 人，改为单独标记（如前缀 `未知`）并告警。

#### P1-5　看板进度：分子分母不配套，且演员名映射失配
- 位置：`library_dashboard.py:285-288` 与 `:57`
- 两处错：
  1. `:57` 用**目录名**匹配 `演員` 列 → `JULIA` / `木下凛々子` 永远匹配不上，
     **京香Julia 与木下的"单人"计数恒为 0**
  2. `:288` 分子用 `d["xhtml"]`（**全部** xhtml，含多人），分母用单人数 → 口径不一致
- 后果：京香Julia 看板显示 `608/1500 = 41%`，真实单人进度是 **448/448 = 100%**。
  橘エレナ显示 `64/255`，真实是 `50/50`。
- ✅ 修复：用第十二节映射表取源站写法；分子改成「`单人_*.xhtml` 数」，
  分母改成「单人 CSV 条数」。

#### P1-6　组级本与「日期未标注」本会重复收录同一部作品
- 位置：`plan_volumes.py:93-98`（组总数 ≤ 50 出组级本，filter = `cat*`）
- 无日期作品的文件名是 `单人_0000-00-00_…`，**会匹配 `单人*`**
- 同时 `日期未标注作品集`（filter `*_0000-00-00*`）也收录 → **同一部进两本书**
- 现状：**当前 8 个演员的数据都没触发**（有无日期作品的组，总数都 > 50 走年份切了），
  但逻辑隐患在，新增演员就会踩
- ✅ 修复：组级本 filter 用 `cat` 时显式排除无日期，或让"日期未标注"优先、
  组级本 filter 改成 `单人_2*` 这类带年份前缀的。

---

### 🟡 P2 — 稳健性与体验

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| P2-1 | `javbus_to_sigil.py:41-42`<br>`sigil_epub_build.py:31-33`<br>`split_actor_csv.py:54` | **默认路径硬编码「中森玲子」**，忘传 `--dir/--out` 就写到中森玲子目录 | 默认值改 `None`，缺失时报错退出 |
| P2-2 | `javbus_to_sigil.py` | 未显式 `import urllib.parse`，靠 `urllib.request` 间接带入 | 显式 import |
| P2-3 | `sigil_epub_build.py:233` | `all_imgs` 全量驻留内存再写 zip，655 部时峰值 500 MB+ | 边解析边写 |
| P2-4 | 全部抓取脚本 | **无文件锁**，两个进程同 out_dir 会重复抓 / 抢限流 | out_dir 下写 `.lock` |
| P2-5 | `sigil_epub_build.py:29` | 全局 `warnings.filterwarnings("ignore")` 掩盖真问题 | 去掉或只过滤指定类别 |
| P2-6 | 文件名 | 最长 419 字节 / 169 字符；APFS 按字符算安全，但 **64 个文件 UTF-8 字节 > 255**，跨平台备份/部分工具有兼容风险 | 标题截断到 ~80 字符 |
| P2-7 | `library_dashboard.py:142` | 扫 `/tmp/*_run*.log` 全部历史日志，看板堆一堆"已停止" | 只取最近 24h / 活跃进程 |
| P2-8 | `split_actor_csv.py:62` | `--mode not-actor`（多人）会把**混入的别演员**也算进去 | 加白名单，只收 `多人` |
| P2-9 | 磁盘 | 可用 22.1 GB（9.7%），项目 6.29 GB 且还在涨 | 见第四节容量规划 |

---

## 三、卡顿与冲突分析

### 3.1 反爬闸门（源站限流）
- 机制：连续请求撞 `/doc/driver-verify`，IP 级，**约 45s 自动解除**
- 脚本退避：45s → 67s → 100s，共 4 次 → **单条最坏约 3.5 分钟**
- 466 条批量若频繁撞闸门，实际耗时可达数小时
- 现有缓解：`--delay 3`；SKILL.md 已记录判定方式（页面无 `class="col-md-3 info"`）
- 🔧 建议：撞闸门时**全局降速**（把 delay 临时提到 8-10s），而不是只靠重试

### 3.2 多会话并发抢限流
- javbus 是 **IP 级**限流，两个批处理同跑会**互相拖慢**，且都更容易撞闸门
- 现有缓解：SKILL.md 七·6 要求先 `pgrep -fl javbus_to_sigil`
- 🔧 建议：脚本层加 `.lock`（P2-4），把人工检查变成硬约束

### 3.3 抓取进程靠会话轮次保活（架构级脆弱点）
- 后台批处理在会话**轮次停止**时会被沙箱回收 —— 这是上一轮北条麻妃跑了 466 条
  却"卡住"的真实原因（不是脚本 bug，是进程被回收）
- 现有缓解：SKILL.md 七·5 的"轻量监视器 + 通知唤醒"写法
- 🔧 建议：长批次改 `launchd` 托管（本机终端装），彻底脱离会话生命周期

### 3.4 脚本循环结束不退出
- 上一轮观察到：`javbus_to_sigil.py` 跑完 466 条后进程不退出
- 定位：`main()` 没有显式 `raise SystemExit`，且 urllib 连接可能未完全释放
- 影响：进程挂着占资源，下批次的 `pgrep` 会误判"有并发在跑"
- 🔧 修复：`main()` 末尾显式 `sys.exit(0)`；`fetch()` 用 `with` 已保证释放，
  可再加 `urllib.request.urlcleanup()`

### 3.5 Sigil 自动打开
- 生成后默认 `open -a Sigil` —— 批量出 46 本书会**开 46 次 Sigil**
- 🔧 批量必须 `--no-open`（SKILL.md 已写，但容易忘）

### 3.6 进度静默 → "很慢很卡"体感
- SKILL.md 七·5 已修（每 10 条同步一次 + 轻量监视器），
  **明确禁止** `TaskOutput(block=true, timeout=600000)`
- 这条已解决，保留为规范即可。

---

## 四、容量规划（当前最硬的约束）

```
磁盘总量      228 GB
已用          206 GB
可用           22.1 GB  (9.7%)   ⚠️
项目当前        6.29 GB
```

**增量预估：**

| 待办 | 预估增量 |
|---|---|
| 北条麻妃补抓 58 部单人 | +0.3 GB |
| 白鳥寿美礼补抓 18 部 | +0.1 GB |
| 中森玲子补抓 10 部 | +0.05 GB |
| 京香Julia 单人卷重建（漏 371 部） | +0.5 GB |
| 凪ひかる / 橘 首次出 epub | +0.15 GB |
| **合计** | **约 +1.1 GB** |

加上 epub 重建时的临时文件（≈成品 1 倍），**峰值需要 ~2.5 GB**。
22.1 GB 够用，但**余量不宽裕**。

🔧 建议：
1. 生成大 epub 前先看 `df -h`，低于 10 GB 先清理
2. P0-1 的"写前预检"必须做，防止撑爆损书
3. 中长期：北原夏美（1.82 GB）+ 京香Julia（1.58 GB）占了一半，
   若磁盘吃紧，优先清理 `/tmp` 与各处的 `.tmp` / `.bak`

---

## 五、加固优先级路线

| 阶段 | 内容 | 风险 | 收益 |
|---|---|---|---|
| **第一批（必做）** | P0-1 epub 原子写 + 磁盘预检<br>P0-3 明细自动带时间戳<br>P1-3 esc 补引号转义 | 低 | 消除不可逆损失 |
| **第二批** | P1-1 占位图判定对齐文档<br>P1-2 单图失败不拖垮整部<br>P1-4 演员空值不归单人 | 低 | 消除静默污染 |
| **第三批** | P1-5 看板进度口径 + 映射表<br>P1-6 组级本去重<br>P0-2 源料版本留档 | 中 | 数据可信、可回滚 |
| **第四批** | P2-1 默认路径<br>P2-4 文件锁<br>P2-3 流式写 zip<br>P2-6 文件名截断 | 中 | 长期稳健 |

---

## 六、本次已落地

1. ✅ 删除源料中混入的 `J-99157`（尾嶋みゆき），北条麻妃源料 2055 → 2054
2. ✅ 按「源料为骨架 + 并集保留」重构 8 个演员主 CSV 与单人 CSV，独立校验 8/8 通过
3. ✅ skill 新增**第十二节**（源料上游 / 演员名映射 / 并集规则）+ 3 个可复用脚本
4. ✅ 本报告

## 七、待陈少决策

1. **脚本要不要现在修？** 我建议先做第一批（P0-1 / P0-3 / P1-3），都是低风险高收益。
2. **磁盘要不要先清？** 22.1 GB 可用，建大 epub 前建议扫一遍 `/tmp`。
3. **源料要不要建版本目录？**（P0-2）这决定了以后能不能回滚。
4. **长批次要不要改 launchd 托管？**（3.3）彻底解决"会话一停进程就没"。
